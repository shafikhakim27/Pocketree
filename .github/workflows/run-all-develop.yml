name: Run All Pipelines (Develop)

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger develop workflows
        uses: actions/github-script@v7
        with:
          script: |
            const workflows = [
              "api-ci.yml",
              "api-dast.yml",
              "ml-service-ci.yml",
              "ml-dast.yml",
              "android-ci.yml",
              "android-ui-test.yml",
              "android-dast.yml",
              "android-mobsfscan.yml",
              "security-report.yml",
              "code-quality.yml",
              "contribution-report.yml",
              "Pipeline Artifacts.yml"
            ];

            for (const wf of workflows) {
              core.info(`Dispatching ${wf} on develop`);
              await github.rest.actions.createWorkflowDispatch({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: wf,
                ref: "develop"
              });
            }
